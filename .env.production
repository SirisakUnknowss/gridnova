# Production analytics — read by Vite at build time (`vite build`).
# The PostHog project key is a publishable client-side key (shipped in the
# browser bundle), so it is safe to commit. A GitHub Actions secret named
# VITE_POSTHOG_KEY, if set, takes precedence over this value.
VITE_POSTHOG_KEY=phc_qQX5fBrwdT5fKnAMy5uziDzAKy8votGwHda45P2HfsRi
VITE_POSTHOG_HOST=https://us.i.posthog.com
